name: Shell Commands

on: [push]

jobs:
  run-shell-command: 
    runs-on: ubuntu-22.04
    steps:
      - name: echo a string
        run: echo "Isso é um teste"
      - name: Multiline script
        run: |
          node -v 
          npmv -v 